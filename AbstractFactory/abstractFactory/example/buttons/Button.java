
package abstractFactory.example.buttons;

/*
 * Abstract Factory assumes that exists several type of family products (Button in this case). Each of them with a common interface
 * This is the Button common interface
 */

public interface Button{
	void paint();
}
